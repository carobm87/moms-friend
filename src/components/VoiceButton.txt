import { useEffect, useRef, useState } from "react";import { Mic, MicOff } from "lucide-react";import { cn } from "@/lib/utils";import { toast } from "sonner";type Props = {  onTranscript: (text: string) => void;  onInterim?: (text: string) => void;  disabled?: boolean;};// Browser SpeechRecognition (free, no API key). Spanish.export function VoiceButton({ onTranscript, onInterim, disabled }: Props) {  const [listening, setListening] = useState(false);  const [supported, setSupported] = useState(true);  const recRef = useRef<any>(null);  // Use refs for callbacks so the recognition instance never needs to be recreated  const listeningRef = useRef(false);  const finalTextRef = useRef("");  const onTranscriptRef = useRef(onTranscript);  const onInterimRef = useRef(onInterim);  // Keep callback refs in sync without triggering recreation of the recognizer  useEffect(() => { onTranscriptRef.current = onTranscript; }, [onTranscript]);  useEffect(() => { onInterimRef.current = onInterim; }, [onInterim]);  useEffect(() => {    const SR = (typeof window !== "undefined" &&      ((window as any).SpeechRecognition || (window as any).webkitSpeechRecognition)) as any;    if (!SR) {      setSupported(false);      return;    }    const rec = new SR();    rec.lang = "es-MX";    rec.continuous = true;    rec.interimResults = true;    rec.onresult = (event: any) => {      let interim = "";      for (let i = event.resultIndex; i < event.results.length; i++) {        const transcript = event.results[i][0].transcript;

        if (event.results[i].isFinal) {          finalTextRef.current += transcript + " ";        } else {          interim += transcript;        }      }      onInterimRef.current?.((finalTextRef.current + interim).trim());    };    rec.onerror = (e: any) => {      console.error("speech error", e);      if (e.error === "not-allowed") {        toast.error("Necesito permiso para usar el micrófono.");        listeningRef.current = false;        setListening(false);      } else if (e.error === "no-speech") {        // iOS fires no-speech quickly — restart if still meant to be listening        if (listeningRef.current) {          try { rec.start(); } catch {}        }      } else if (e.error !== "aborted") {        toast.error("No te escuché bien. Intenta de nuevo.");        listeningRef.current = false;        setListening(false);      }    };    rec.onend = () => {      // iOS Safari stops automatically after a short pause.      // If the user hasn't tapped stop, restart immediately.      if (listeningRef.current) {        try {          rec.start();          return; // still recording — don't fire transcript yet        } catch (e) {          console.error("restart failed", e);        }      }      // User tapped stop (or restart failed) — deliver the transcript      setListening(false);      const text = finalTextRef.current.trim();      if (text) onTranscriptRef.current(text);      finalTextRef.current = "";    };    recRef.current = rec;

    return () => {      try { rec.abort(); } catch {}    };  }, []); // Empty deps — create the recognizer only once  if (!supported) return null;  const toggle = () => {    if (disabled) return;    if (listeningRef.current) {      // User wants to stop      listeningRef.current = false;      setListening(false);      recRef.current?.stop();    } else {      // User wants to start      finalTextRef.current = "";      listeningRef.current = true;      try {        recRef.current?.start();        setListening(true);      } catch (e) {        console.error(e);        listeningRef.current = false;        setListening(false);      }    }  };  return (    <button      type="button"      onClick={toggle}      disabled={disabled}      aria-label={listening ? "Detener grabación" : "Hablar"}      className={cn(        "h-14 w-14 shrink-0 rounded-full flex items-center justify-center transition-all shadow-md",        listening          ? "bg-destructive text-destructive-foreground scale-110 animate-pulse"          : "bg-warm text-primary hover:bg-warm/80"      )}    >      {listening ? <MicOff className="h-6 w-6" /> : <Mic className="h-6 w-6" />}    </button>  );}